# ──────────────────────────────────────────────────────────────────────────────
# DocAuth — Environment Configuration
# Copy this file to .env and fill in the values.
# NEVER commit your real .env file to version control.
# ──────────────────────────────────────────────────────────────────────────────

# ── Model weights ─────────────────────────────────────────────────────────────
# Path to pretrained Siamese network weights (downloaded or trained locally)
SIGNATURE_WEIGHTS_PATH=weights/siamese_best.pt

# ── HuggingFace ───────────────────────────────────────────────────────────────
# Optional: set your HuggingFace token to access gated models
# HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── OCR ───────────────────────────────────────────────────────────────────────
# GPU device for EasyOCR and TrOCR (set to -1 for CPU)
OCR_GPU=0

# ── Streamlit ─────────────────────────────────────────────────────────────────
# Max upload size in MB (also set in .streamlit/config.toml)
# STREAMLIT_SERVER_MAX_UPLOAD_SIZE=50

# ── Logging ───────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO
